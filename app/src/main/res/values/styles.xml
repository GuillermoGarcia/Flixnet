<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--
        Creamos un estilo pesonalizado para nuestra barra de rating, de manera que
        aparezca más pequeña que si utilizamos su configuración normal
     -->
    <style name="myOwnRatingBar" parent="Widget.AppCompat.RatingBar.Small" />

</resources>
